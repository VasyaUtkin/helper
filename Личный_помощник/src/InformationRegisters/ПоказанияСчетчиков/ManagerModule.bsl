Функция ПолучитьПоказанияСчетчиков(Знач Услуги = Неопределено, Знач Дата = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПоказанияСчетчиков.Значение,
		|	ПоказанияСчетчиков.ВидУслуги
		|ИЗ
		|	РегистрСведений.ПоказанияСчетчиков.СрезПоследних(&Дата, ВидУслуги В (&Услуги)) КАК ПоказанияСчетчиков";
		
	Если Не ЗначениеЗаполнено(Услуги) Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "ВидУслуги В (&Услуги)", "");		
	КонецЕсли;
		
	Запрос.УстановитьПараметр("Дата", Дата);
	Запрос.УстановитьПараметр("Услуги", Услуги);
	
	Возврат Запрос.Выполнить().Выгрузить();
		
КонецФункции