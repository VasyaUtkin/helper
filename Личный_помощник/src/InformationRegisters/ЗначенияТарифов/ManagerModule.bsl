Функция ПолучитьЗначенияТарифов(Знач Услуги = Неопределено, Знач Дата = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ЗначенияТарифовСрезПоследних.Значение,
		|	ЗначенияТарифовСрезПоследних.ВидУслуги
		|ИЗ
		|	РегистрСведений.ЗначенияТарифов.СрезПоследних(&Дата, ВидУслуги В (&Услуги)) КАК ЗначенияТарифовСрезПоследних";
		
	Если Не ЗначениеЗаполнено(Услуги) Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "ВидУслуги В (&Услуги)", "");		
	КонецЕсли;
		
	Запрос.УстановитьПараметр("Дата", Дата);
	Запрос.УстановитьПараметр("Услуги", Услуги);
	
	Возврат Запрос.Выполнить().Выгрузить();
		
КонецФункции
